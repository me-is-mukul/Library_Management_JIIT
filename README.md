# Library_Management_JIIT
Library Management System Based on Ncurses in C (with TERMINAL USER INTERFACE )

## to run it u have to paste the following command

```
gcc main.c -lncurses -lmenu devs/ambar.c devs dhvani.c passwords/password.c
```
